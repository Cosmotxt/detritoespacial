import { use, useState } from "react"

const Logotipo = () => {

    return(
        <div>
            <img src={logo} alt='logo da detrito espacial' className='w-[50vw] h-auto mx-auto mt-[10vh]'/>
        </div>
    )
}
